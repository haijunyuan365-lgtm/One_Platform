# 详细设计章节标准结构

## 每个功能模块（3.5.x）的完整结构

```
#### 3.5.x.x 功能名称

**（一）功能需求概述**
[1-3句话说明该功能的目的和价值，不写实现方式]

**（二）功能设计**

**[子功能1]功能**
[功能说明段落]

[字段表格（如有）]

**业务规则**
[触发条件 + 影响范围 + 提示文案原文]

**边界与异常**
[异常场景 + 处理方式 + 提示文案原文]

**（三）业务逻辑关联**
[依赖哪些其他模块的数据，跨模块影响说明]

**（四）用户操作流程图**
![](images/[功能名]-operation-flow.png)

**（五）功能时序图**
![](images/[功能名]-sequence.png)
```

---

## 各部分写法规范

### （一）功能需求概述

- 1-3 句话，说明"这个功能解决什么问题"、"谁在用"、"核心价值是什么"
- 不写页面布局，不写技术实现

```
✅ 示例：
设备档案管理功能用于维护厂区所有设备的基本信息，支持按分类、单位、状态多维度查询，
为巡检计划配置和设备状态监控提供基础数据支撑。
```

---

### （二）功能设计 — 筛选功能

```markdown
**查询/筛选功能**

支持以下条件筛选，条件之间为"与"关系，点击搜索按钮触发查询：

| 筛选字段 | 类型 | 说明 |
|---------|------|------|
| 字段名 | 文本输入 | 支持模糊搜索 |
| 字段名 | 单选 | 枚举值：A/B/C |
| 字段名 | 层级选择 | 来源于[模块]中已启用的[数据] |
| 字段名 | 日期范围 | 选择开始和结束日期 |
```

---

### （二）功能设计 — 列表功能

```markdown
**列表功能**

| 列名 | 字段类型 | 说明 |
|-----|---------|------|
| 字段名 | 文本 | - |
| 字段名 | 状态标签 | 启用（绿色）/停用（灰色） |
| 字段名 | 图片+文本 | 左侧缩略图，点击可预览大图；无图显示占位图 |
| 字段名 | 文本 | 无值显示"-" |
| 操作 | 操作按钮 | 编辑、删除（无权限时隐藏） |

分页默认每页10条，支持切换每页数量。
支持批量勾选，勾选后操作栏显示批量删除按钮（含已选数量）。
列表为空时显示：暂无[数据名]数据。
```

---

### （二）功能设计 — 新增/编辑功能

```markdown
**新增功能**

点击"新增"进入新建[数据名]页面（或弹出编辑窗口）。

| 字段名称 | 类型 | 长度限制 | 必填 | 默认值 | 说明 |
|---------|------|---------|------|-------|------|
| 字段名 | 文本输入 | 2-50字 | 是 | - | 说明 |
| 字段名 | 单选 | - | 是 | 启用 | 枚举值：A/B/C |
| 字段名 | 层级选择 | - | 是 | - | 来源于[模块]中已启用的[数据] |
| 字段名 | 图片上传 | 最多5张 | 否 | - | 单张最大10MB |

提交成功：[成功提示文案原文]。
提交失败：保留填写内容，显示具体错误原因。

**编辑功能**

点击"编辑"，自动填入当前记录数据，表单字段同新增。
提交成功：[成功提示文案原文]。
```

---

### （二）功能设计 — 删除功能

```markdown
**删除功能**

点击"删除"，弹出确认"[确认文案原文]"。
删除成功：[成功提示文案原文]。
[如有关联保护：有关联数据时阻止删除，提示"[提示文案原文]"。]
```

---

### （二）功能设计 — 业务规则

**必须包含三要素：触发条件 + 影响范围 + 提示文案原文**

```markdown
**业务规则**

- [字段名]在系统内全局唯一。新增/编辑时若重复，阻止提交，提示"[提示文案原文]"。
- [状态]变更为[值]后，[跨模块影响说明，用业务语言描述]。
- 删除[数据名]时，若存在关联[数据名]，阻止删除，提示"[提示文案原文]"。
```

---

### （二）功能设计 — 边界与异常

```markdown
**边界与异常**

- **空数据**：列表无数据时，显示"[提示文案原文]"
- **权限不足**：[操作]按钮对无权限角色完全隐藏（不渲染，非禁用）
- **[其他异常场景]**：[处理方式和提示文案]
```

---

### （三）业务逻辑关联

```markdown
**（三）业务逻辑关联**

- [字段名]数据来源于[模块名]，需要[模块名]先完成配置
- [本模块]的[状态/数据]变更后，影响[其他模块]的[具体行为]
- [本模块]被[其他模块]引用时，删除需先解除关联
```

---

## 章节完整性检查

每个功能模块写完后，对照以下清单：

- [ ] （一）功能需求概述：有，且不含技术词汇
- [ ] 筛选功能：有字段表格，来源描述用业务语言
- [ ] 列表功能：有字段表格，含分页说明、空数据提示
- [ ] 新增/编辑功能：有字段表格，含必填/长度/默认值/提示文案
- [ ] 删除功能：有确认文案原文，有关联保护说明（如有）
- [ ] 业务规则：有触发条件 + 影响范围 + 提示文案原文
- [ ] 边界与异常：有空数据、权限不足场景
- [ ] （三）业务逻辑关联：有跨模块依赖说明
- [ ] （四）（五）流程图占位：有图片引用
